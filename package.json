{
  "name": "regle-examples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "pnpm run -r --filter=* typecheck "
  },
  "engines": {
    "pnpm": ">=8.7.0"
  }
}
